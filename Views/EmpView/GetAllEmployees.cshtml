@model IEnumerable<employee_management_agile.Models.EmpModel>

<h2>Employee List</h2>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Position</th>
            <th>Department</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
        {
            <tr>
                <td>@employee.Name</td>
                <td>@employee.Position</td>
                <td>@employee.Department</td>
                <td>
                    @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                    {
                        <a asp-action="RoleUpdate" asp-route-id="@employee.Id" class="btn btn-sm btn-warning">Update Role</a>
                    }
                    @if (User.IsInRole("Admin") || User.IsInRole("Manager") || User.IsInRole("Employee"))
                    {
                    <a asp-action="DetailsEmployee" asp-route-id="@employee.Id" class="btn btn-sm btn-info">Details</a>
                    }
                    @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                    {
                    <a asp-action="EditEmployee" asp-route-id="@employee.Id" class="btn btn-sm btn-primary">Edit</a>
                    }
                    @if (User.IsInRole("Admin"))
                    {
                    <a asp-action="DeleteEmployee" asp-route-id="@employee.Id" class="btn btn-sm btn-danger">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="CreateEmployee" class="btn btn-success">Create New Employee</a>